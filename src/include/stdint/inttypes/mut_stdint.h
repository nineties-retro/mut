#ifndef MUT_STDINT_H
#define MUT_STDINT_H

/*
 * OpenBSD 2.8 doesn't have a <stdint.h> but it does have most of the
 * contents of what would in <stdint.h> in <machine/types.h> which in
 * turn is included by ...  */

#include <inttypes.h>

#endif
